# Prometheus configuration — wired into Docker Compose in Phase 5.
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: shardleak
    static_configs:
      - targets:
          - api:8080
